export type TodoType = {
  title: string;
  titlePlaceholder: string;
  text: string;
  textPlaceholder: string;
  done: boolean;
  id: string;
  active: boolean;
};

export enum Items {
  TODO = 'todo',
}
